import * as types from './../types';

const initialState = {
    login: 'admin',
    pass: '123',
};

export default function userReducer(state = initialState, action) {
    switch (action.type) {
        
        default:
            return state
    }
}